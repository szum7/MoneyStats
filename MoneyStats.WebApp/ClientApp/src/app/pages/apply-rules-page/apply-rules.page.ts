import { Component, Input, Output, OnInit } from '@angular/core';

@Component({
    selector: 'app-apply-rules-page',
    templateUrl: './apply-rules.page.html',
    styleUrls: ['./apply-rules.page.scss']
})
export class ApplyRulesPage implements OnInit {

    constructor() {
    }

    ngOnInit(): void {
    }
}
